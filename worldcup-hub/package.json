{
  "name": "worldcup-hub",
  "version": "0.0.0",
  "private": true,
  "description": "World Cup 2026 Live Hub — monorepo (Author: Bishakh)",
  "packageManager": "pnpm@10.28.1",
  "scripts": {
    "dev": "turbo run dev",
    "dev:web": "turbo run dev --filter=@wc/shell --filter=@wc/mfe-scoreboard --filter=@wc/mfe-match-center --filter=@wc/mfe-standings --filter=@wc/mfe-news",
    "build": "turbo run build",
    "preview": "turbo run preview",
    "typecheck": "turbo run typecheck"
  },
  "devDependencies": {
    "turbo": "^2.5.0",
    "typescript": "^5.7.3"
  }
}
